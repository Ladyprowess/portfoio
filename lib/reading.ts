/**
 * Scroll offset under which the reader still counts as sitting on an article's
 * title screen. Shared so the scroll cue and the snap-to-body gesture agree:
 * whenever the jump is armed, the cue that advertises it is on screen.
 */
export const TITLE_SCREEN_THRESHOLD = 24
